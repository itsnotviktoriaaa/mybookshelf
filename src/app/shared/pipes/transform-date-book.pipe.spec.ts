import { TransformDateBookPipe } from 'app/shared';

describe('TransformDateBookPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformDateBookPipe();
    expect(pipe).toBeTruthy();
  });
});
